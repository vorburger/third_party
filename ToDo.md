# ToDo

1. [Gemini](https://gemini.google.com/app/359f96bf9c19adde)

1. `direnvrc` with `source_env_if_exists .envrc.private` in my dotfiles

1. `.envrc.private` in my dotfiles' on `~/.config/git/ignore`

1. inside a 3p project's `.envrc.private` put:
   `use flake ~/git/github.com/vorburger/third_party/github.com/x/y`
