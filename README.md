# third_party

This repo contains ❄️ `flake.nix` for various _"third party"_ projects.

That makes it easy to contribute and work on those projects, like this:

    TODO

**Nota bena:** This is for projects which are NOT already in
[`nixpkgs`](https://github.com/NixOS/nixpkgs).
For project that are, you can simply do:

    nix develop nixpkgs#ssh-tpm-agent

[ToDo](ToDo.md)
